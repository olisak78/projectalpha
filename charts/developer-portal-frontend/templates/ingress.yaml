{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "developer-portal-frontend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "developer-portal-frontend.labels" . | nindent 4 }}
  annotations:
    cert.gardener.cloud/purpose: managed
    cert.gardener.cloud/issuer: sapca
    dns.gardener.cloud/class: garden
    dns.gardener.cloud/dnsnames: {{ .Values.ingress.host }}
    kubernetes.io/ingress.class: nginx
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "developer-portal-frontend.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
  tls:
    - hosts:
        - {{ .Values.ingress.host }}
      secretName: {{ .Values.ingress.tlsSecretName | default "developer-portal-frontend-tls" }}
{{- end }}

