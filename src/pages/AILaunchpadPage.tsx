import React from 'react';
import { DeploymentsManager } from '@/components/AILaunchpad/DeploymentsManager';

const AILaunchpadPage: React.FC = () => {
  return (
    <div className="ai-launchpad-page">
      <DeploymentsManager />
    </div>
  );
};

export default AILaunchpadPage;
